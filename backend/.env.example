# ============================================================================
# Filmer - Environment Variables Template
# ============================================================================
# INSTRUCTIONS:
# 1. Copy this file and rename it to .env (it's already in .gitignore)
# 2. Update the values below with your actual credentials
# 3. NEVER commit the .env file to version control
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# These values should match your Docker Compose PostgreSQL service
# or your local PostgreSQL installation if not using Docker

# Database connection URL
# Format: jdbc:postgresql://host:port/database_name
# For Docker: use 'localhost' (Docker maps port 5432 to your host)
# For local PostgreSQL: use 'localhost' or your server address
DB_URL=jdbc:postgresql://localhost:5432/filmer

# Database username
DB_USER=postgres

# Database password
# IMPORTANT: Change this in production!
# For Docker: This should match POSTGRES_PASSWORD in docker-compose.yml
DB_PASSWORD=filmer_dev_password

# ============================================================================
# FUTURE CONFIGURATION (for later phases)
# ============================================================================

# JWT Secret (uncomment when implementing authentication)
# JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# API Keys (uncomment when needed)
# TMDB_API_KEY=your-tmdb-api-key
# AWS_ACCESS_KEY=your-aws-access-key
# AWS_SECRET_KEY=your-aws-secret-key

# ============================================================================
# NOTES
# ============================================================================
# - This file is in .gitignore and will NOT be committed
# - Each developer should have their own .env file
# - For production, use proper secret management (AWS Secrets Manager, etc.)
# - Never share your .env file in chat, email, or screenshots
# ============================================================================
